// Re-exported from `@pavisie/types/integrations` (the single source of truth for this list) so existing
// imports of `../lib/integrations/outbound-events` elsewhere in this app keep working unchanged.
export { OUTBOUND_PLATFORM_EVENTS, type OutboundPlatformEvent } from '@pavisie/types/integrations';
