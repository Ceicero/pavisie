{
  "name": "pavisie",
  "private": true,
  "license": "AGPL-3.0-only",
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.9",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm -r --parallel --filter \"./apps/*\" run dev",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "test:e2e": "pnpm --filter @pavisie/web test:e2e",
    "build": "pnpm -r run build",
    "db:generate": "pnpm --filter @pavisie/database generate",
    "db:migrate": "pnpm --filter @pavisie/database migrate:deploy",
    "db:migrate:dev": "pnpm --filter @pavisie/database migrate:dev",
    "db:seed": "pnpm --filter @pavisie/database seed",
    "commands:register": "pnpm --filter @pavisie/bot register",
    "commands:export": "pnpm --filter @pavisie/plugins export:commands",
    "docs:permissions": "pnpm --filter @pavisie/plugins export:permissions",
    "check:i18n": "pnpm --filter @pavisie/plugins check:i18n",
    "brand:sync": "node scripts/sync-brand.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@discordjs/rest": "^2.6.3",
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.0",
    "discord-api-types": "^0.38.53",
    "dotenv": "^16.6.1",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.19.0",
    "vitest": "^3.0.0"
  }
}
